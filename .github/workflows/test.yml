name: Test React Vite Project
on: push
jobs:
  test-react:
    runs-on: ubuntu-latest
    steps:
      - name: Pull Code
        uses: actions/checkout@v5
      - name: Install NodeJS
        uses: actions/setup-node@v5
        with:
          node-version: 24
      - name: Install Dependencies
        run: npm ci # Install all dependencies
      - name: Run Tests
        run: npm test